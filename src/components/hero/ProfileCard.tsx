import type { User } from "@/types";

interface ProfileCardProps {
  user: User;
}

export default function ProfileCard({ user: _user }: ProfileCardProps) {
  // TODO: HTML'deki .profile-card bloğunu buraya taşı
  return <div />;
}
